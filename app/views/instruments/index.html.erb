<div class="container">
  <hr class="my-4">
        <div>
                <%# <%= link_to "All Enrollments", enrollments_path %>
                <%# <%= link_to "Create a New Enrollment", new_enrollment_path %>|
                <%= link_to "Add New Instrument", new_instrument_path, class: "btn btn-warning text-white" %> |
                <%# <%= link_to "Logout", logout_path, method: 'delete' %>
        </div>
  <h2 class="font-weight-lighter">List of All Instruments</h2> 
  <br>        
        <table class="table table-striped table-bordered table-hover">
                <thead class="thead-light">
                        <tr>
                                <th>All Instruments</th>
                        </tr>
                <thead>
                <tbody>
                        <% @instruments.each do |instrument| %>
                                <tr>
                                <td><%=instrument.name%></td> 
                                </tr>
                        <% end %>
                </tbody>        
        </table>
 <hr class="my-4">
</div>
        <%# <%= link_to 'Add New Enrollment', new_enrollment_path, class: "btn btn-warning text-white" %><br><br>