<div class="container">
  <hr class="my-4">
        <div>
                
                <%= link_to "Add New Instrument", new_instrument_path, class: "btn btn-warning text-white" %> |
                
        </div>
 <h2 class="font-weight-lighter">List of All Instruments</h2> 
  <br>        
        <table class="table table-striped table-bordered table-hover">      
                <thead class="thead-light" <%#= display_header(@instrument)%> >
                        <tr>
                          <th>All Instruments</th>
                        </tr>
                </thead>
                <tbody>
                        <% @instruments.all.each do |instrument| %></td>

                                <tr>
                                <td><%= link_to instrument.name, instrument_enrollments_path(instrument)%></td>                    
                                </tr>
                        <% end %>
                </tbody> 
        </table> 
  <hr class="my-4">
</div>      
